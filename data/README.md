# 数据目录

这里存放采样清单和本地采样输出。

```text
data/
  plans/      # 采样清单 CSV，建议上传 GitHub
  raw/        # 本地采样输出，默认不上传 GitHub
```

建议把 `data/plans/*.csv` 放进仓库，这样队友能知道每根实体线的编号和标签。`data/raw/` 里的完整采样数据先留在本地，除非队伍决定上传一个小样例数据集。
